server:
  port: 8888

spring:
  application:
    name: config
  cloud:
    config:
      server:
        git:
          uri: https://github.com/tomekceszke/hello-cloud-config
  output:
    ansi:
      enabled: ALWAYS


